protoc:
  version: 3.11.0

lint:
  group: uber2

generate:
  go_options:
    import_path: github.com/scizorman/aws-playground/backend
  plugins:
    - name: gogo
      type: gogo
      flags: plugins=grpc
      output: ../internal/interfaces/rpc
